# P2P Analysis - Chrome Extension

Расширение для анализа P2P заказов на платформах Bybit и HTX.

## Поддерживаемые платформы

- ✅ **Bybit** - страницы заказов (`/fiat/trade/otc/item/orderList/`)
- ✅ **HTX** - страницы заказов (`/fiat-crypto/tradeInfo/`)

## Основные функции

### Общие функции
- 📊 Выбор банковских реквизитов из списка
- 💰 Указание комиссии (в % или фиксированной сумме в рублях)
- 💾 Сохранение данных заказа на сервер
- 🗑️ Удаление заказа из базы данных
- 🔄 Автоматическая предзаполнение для существующих заказов
- 🔔 Уведомления об успешных/ошибочных операциях

### Специфические функции Bybit
- 📋 Анализ списка заказов в таблице
- 🎨 Подсветка заказов с комиссией
- 📱 Поддержка мобильной и десктоп версии

### Специфические функции HTX
- 🎯 Интерфейс в стилистике HTX (синяя цветовая схема #0173E5)
- 📍 Размещение интерфейса под блоком с чатом
- 🔍 Автоматическое определение типа заказа (покупка/продажа)

## Установка

1. Склонируйте репозиторий
2. Откройте Chrome и перейдите в `chrome://extensions/`
3. Включите "Режим разработчика" (Developer mode)
4. Нажмите "Загрузить распакованное расширение" (Load unpacked)
5. Выберите папку с расширением

## Использование

### На Bybit

1. Откройте страницу заказа P2P (URL: `https://www.bybit.com/.../orderList/[ID]`)
2. Интерфейс расширения появится под блоком с чатом
3. Выберите банк из выпадающего списка
4. Введите комиссию (% или ₽)
5. Нажмите "Сохранить заказ"

### На HTX

1. Откройте страницу заказа P2P (URL: `https://www.htx.com/.../tradeInfo/?order=[ID]`)
2. Интерфейс расширения появится под блоком с чатом
3. Выберите банк из выпадающего списка
4. Введите комиссию (% или ₽)
5. Нажмите "Сохранить заказ"

## Отладка

### Проверка загрузки скрипта

Откройте консоль разработчика (F12) и проверьте наличие следующих сообщений:

**Для Bybit:**
```
P2P Analytics: Content script loaded
P2P Analytics: Current URL: ...
P2P Analytics: On correct page: true
```

**Для HTX:**
```
P2P Analytics HTX: Content script loaded
P2P Analytics HTX: Current URL: ...
P2P Analytics HTX: On correct page: true
```

### Частые проблемы

1. **Интерфейс не появляется:**
   - Проверьте URL страницы
   - Откройте консоль и проверьте логи
   - Убедитесь, что вы авторизованы в popup расширения

2. **Ошибка "Not authenticated":**
   - Откройте popup расширения (иконка в панели браузера)
   - Введите логин и пароль
   - Нажмите "Войти"

3. **Банки не загружаются:**
   - Проверьте подключение к интернету
   - Проверьте в консоли наличие ошибок API
   - Убедитесь, что токен авторизации валиден

## Структура файлов

```
├── manifest.json       # Манифест расширения
├── auth.js            # Модуль авторизации (общий для всех платформ)
├── content.js         # Основной скрипт для Bybit
├── htx.js             # Скрипт для HTX
├── order_list.js      # Анализ списка заказов (только Bybit)
├── background.js      # Service worker
├── popup.html         # HTML popup окна
├── popup.js           # Логика popup окна
├── style.css          # Общие стили
└── icons/             # Иконки расширения
```

## API

Расширение использует следующие endpoints:

- `GET /api/v1/bank-details` - Получение списка банковских реквизитов
- `GET /api/v1/orders/:orderId` - Проверка существования заказа
- `POST /api/v1/orders` - Создание/обновление заказа
- `DELETE /api/v1/orders/:orderId` - Удаление заказа

## Разработка

### Добавление новой платформы

1. Создайте новый файл `[platform].js`
2. Скопируйте структуру из `htx.js`
3. Адаптируйте селекторы под новую платформу
4. Добавьте URL паттерн в функцию `initialize()`
5. Обновите `manifest.json`:
   ```json
   {
     "matches": ["*://[platform].com/*"],
     "js": ["auth.js", "[platform].js"],
     "css": ["style.css"]
   }
   ```

## Лицензия

MIT
