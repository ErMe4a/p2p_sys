## 1. Excel-экспорт (`export_excel_report`, `orders/views.py`)

- [ ] 1.1 Добавить заголовок новой колонки N: `ws.merge_cells('N2:N3')`, `ws['N2'] = "Комиссия биржи, эквивалент
      в руб"` — рядом с заголовками остальных одиночных колонок (A2:A3 и т.п., строка ~2121).
- [ ] 1.2 Расширить стилизацию заголовков `for row in ws.iter_rows(min_row=1, max_row=3, min_col=1, max_col=13)`
      → `max_col=14` (строка ~2134), и `ws.merge_cells('A1:M1')` → `ws.merge_cells('A1:N1')` (строка ~2116).
- [ ] 1.3 В цикле записи строк ордеров (строка ~2249–2270): для BUY-строк добавить 14-й элемент `0`; для
      SELL-строк добавить 14-й элемент `exch_comm_usdt * price_f if exch_rate > 0 else 0`.
- [ ] 1.4 `write_carry_row` (строка ~2275): добавить 14-й элемент `0`.
- [ ] 1.5 `write_summary` (строка ~2296): добавить в "Торговый результат" `f"=SUM(N{data_start}:N{data_end})"`
      четырнадцатым элементом.
- [ ] 1.6 `write_summary`, финальная формула прибыли (строка ~2362): заменить
      `f"=K{rr}-G{rr}-H{tr}-L{tr}-M{tr}*J{rr}"` на `f"=K{rr}-G{rr}-H{tr}-L{tr}-N{tr}"`.

## 2. Python-расчёты для админ-кэша (`orders/views.py`)

- [ ] 2.1 `_calc_all_months_currency_from_orders` (~строка 2887–2975): убрать отдельный проход за
      `last_sell_price`; копить `exch_comm_rub_month` внутри существующего цикла по `month_list`, одновременно с
      `exch_qty` (комиссия в USDT для остатка остаётся как есть).
- [ ] 2.2 `_calc_currency_from_orders` (~строка 3036–3135): то же самое изменение, тот же паттерн.
- [ ] 2.3 Убедиться, что переменная `last_sell_price` больше нигде не используется в этих двух функциях после
      правки (удалить, если стала мёртвым кодом) — только в самом `export_excel_report` она остаётся (там нужна
      для отображения курса в строке "Реализованный результат", не для расчёта комиссии).

## 3. Проверка согласованности (до деплоя, на реальных данных прод-БД, read-only)

- [ ] 3.1 Взять пользователя и месяц с разными курсами продаж и ненулевой `exchange_commission_rate`
      (gnusarev, сентябрь 2026 — уже есть под рукой) — посчитать эталон: сумму `комиссия_usdt_i × курс_i`
      вручную по ордерам.
- [ ] 3.2 Сравнить с результатом `_calc_currency()` (эталонная реализация, не менялась) — подтвердить, что ручной
      расчёт совпадает (это калибровка независимо от фикса).
- [ ] 3.3 После правки — сравнить новый результат `_calc_all_months_currency_from_orders` и
      `_calc_currency_from_orders` с тем же эталоном.

## 4. Деплой и пересчёт кэша

- [ ] 4.1 `git pull` → `restart p2p` + `restart celery celery-sync celery-receipt celerybeat` (правка касается
      функций, которые вызывает `tasks.py`).
- [ ] 4.2 Разово запустить `recompute_yearly_profit_summary_task` и `recompute_admin_profit_cache_task` за
      весь актуальный диапазон (с `SYSTEM_START` по текущий месяц) через `manage.py shell`.
- [ ] 4.3 Сверить цифры годовой/помесячной сводки в админке до и после пересчёта для пары пользователей с
      разными курсами в периоде — убедиться, что числа изменились именно в сторону эталона (шаг 3.1), а не куда-то
      ещё.
- [ ] 4.4 Проверить логи (`journalctl -u p2p`, `-u celery-receipt`) на ошибки после первого прогона задач
      пересчёта.

## 5. Сообщить Максиму

- [ ] 5.1 Показать итоговую цифру по gnusarev/сентябрь до и после фикса — наглядное подтверждение, что теперь
      Excel/админ-кэш совпадают с курсом реальных сделок, а не с курсом последней.
- [ ] 5.2 Уточнить у Максима открытый вопрос из design.md (нужно ли отдельно оповещать об изменившихся задним
      числом цифрах прибыли за прошлые периоды).
